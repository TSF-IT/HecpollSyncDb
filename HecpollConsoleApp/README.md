# Hecpoll.Sync

La documentation principale est maintenant a la racine du depot (`../README.md`).

Ce dossier contient le projet console `.NET 8` et la documentation detaillee reste dans `DOCS/Hecpoll.Sync-Overview.md`.
